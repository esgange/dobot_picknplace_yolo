# orbbec_camera_msgs

ROS 2 interface definitions for the Orbbec camera driver. It contains camera status, stream-profile, metadata, RGB-D, filter, calibration, and device-control messages and services.

This package has no direct hardware side effects; it is built as a dependency of `orbbec_camera`.
