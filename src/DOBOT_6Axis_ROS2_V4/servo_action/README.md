# servo_action

Joint trajectory action client utilities for the Dobot CR10. The package sends trajectory goals to the controller interface used by the MoveIt integration.

Use it only with a known controller target and after completing the hardware safety checks. Planning-only and simulated workflows should be preferred during development.
