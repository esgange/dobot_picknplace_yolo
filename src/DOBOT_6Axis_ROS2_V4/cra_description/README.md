# cra_description

CR10 robot description package. It provides the hardware-only CR10 URDF/Xacro and visual/collision meshes consumed by the read-only actual-feedback RViz viewer.

The retained model entry point is `urdf/cr10_robot.xacro`. This project profile intentionally contains no other Dobot robot descriptions.
