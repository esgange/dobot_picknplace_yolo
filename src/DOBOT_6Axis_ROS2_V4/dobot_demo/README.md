# dobot_demo

Python demonstration nodes for the Dobot ROS 2 services. The package is intended for controlled development experiments after the driver and message packages are built.

Run the example entry point with `ros2 run dobot_demo demo` after the driver and message packages are built. Do not use a demo against a physical robot until the hardware safety checks in the project diary are complete.
