# dobot_msgs_v4

ROS 2 interface definitions for the Dobot V4 driver. The package contains the robot status and tool-vector messages plus the command, motion, I/O, and configuration service definitions consumed by the other Dobot packages.

This package has no hardware side effects; it can be built and inspected independently.
