# dobot_kinematics_plugin

MoveIt kinematics plugin for the Dobot CR10. It provides the forward and inverse kinematics implementation loaded by the CR10 MoveIt configuration.

The plugin is a shared runtime dependency of `cr10_moveit`; it is normally exercised through MoveIt planning rather than launched directly.
