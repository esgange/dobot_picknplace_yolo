# Project-wide runtime configuration.
# Copy this file to .env and edit values for the machine/robot in use:
#   cp .env.example .env
#
# .env is intentionally ignored by Git. The bringup launch requires this file
# and does not fall back to shell variables or param.json for the robot address.
# Use strict KEY=value syntax; do not add export prefixes or alternate keys.

# Dobot CR10 controller IP address.
DOBOT_ROBOT_IP=192.168.5.1
